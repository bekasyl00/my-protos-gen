name: Generate Proto Go Files

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Generator Repository
        uses: actions/checkout@v4

      - name: Install Protocol Buffers Compiler
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install protoc plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

      - name: Clone Proto Files Repository
        run: |
          rm -rf proto-source
          git clone --depth 1 https://github.com/bekasyl00/my-protos.git proto-source

      - name: Generate Go Code from Proto Files
        run: |
          find proto-source/proto -name "*.proto" -type f | while read file; do
            protoc \
              --proto_path=proto-source \
              --go_out=. \
              --go-grpc_out=. \
              "$file"
          done

      - name: Cleanup source directory
        run: rm -rf proto-source

      - name: Commit and Push Generated Files
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config --global user.name "bekasyl00"
          git config --global user.email "bekajaksylyk07@gmail.com"

          git add "*.pb.go" || true
          git commit -m "chore: auto-generate pb.go files" || echo "No changes to commit"
          git push https://x-access-token:${PAT_TOKEN}@github.com/bekasyl00/my-protos-gen.git HEAD:main